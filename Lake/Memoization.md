This is the optimization technique that **stores** results of the expensive operations **in memory**.

The example, is doctrine's [[Architecture/Design Patterns/Identity map]] that stores all the objects accessed from the database.
