https://github.com/sezanzeb/input-remapper

Tool that allows to remap keyboard on Ubuntu (with Wayland support)

To find key names (e.g. `Shift_L`), you can use `xev` that will show them interactively as you type: `xev -event keyboard`
