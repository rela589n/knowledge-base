Try to [[Interrupting the Workflow|interrupt the Workflow]] for [[Temporal Booking System]] task.
See if error is thrown from the workflow, or not. Make sure that [[SAGA|compensations]] are executed.
