You can see [[Generator-based coroutines]] for more understanding.

In [[Temporal SDK]], this is implemented in `\Temporal\Internal\Workflow\Process\Scope::next()`.

It's `DeferredGenerator` that is used as a [[Coroutine]].