Use `spiral/roadrunner-cli` package, and then download:

```shell
./vendor/bin/rr get-binary
```

For [[Docker]] installation, use:

```dockerfile
FROM spiralscout/roadrunner:2024.3.5 AS roadrunner
```
