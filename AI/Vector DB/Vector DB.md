**Vector DB** is a database that stores data along with [[Embedding Vector|Vectors]],
optimized for similar [[Embedding Vector|Vector]] search.

> It's like [[Full Text Search]] which can now <u>search by meaning</u> rather than just words or synonyms.

These [[Embedding Vector|Vectors]] themselves are never sent to the LLM.
	Data is.

What [[Chunk|Chunks]].