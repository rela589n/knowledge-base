---
aliases:
  - grammar
---
A [[userInput()|userInput()]] annotation that controls how Vespa parses user input into query terms.

```
{defaultIndex:"title", grammar:"all"}userInput(@user-query)
```

See [[model.type]] for the full list of parsing modes.
