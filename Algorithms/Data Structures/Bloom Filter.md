**Bloom Filter** - data structure for **approximating** the contents of a set using **bitmasks**.

It can possibly tell that the key might exist in the set.
It can for sure tell if the key doesn't exist.
